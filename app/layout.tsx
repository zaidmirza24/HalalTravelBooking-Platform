import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'HalalTravel - Islamic Travel Booking Platform',
  description: 'Find and book halal-friendly hotels and travel packages worldwide',
  keywords: ['halal travel', 'islamic hotels', 'muslim-friendly', 'halal hotels', 'travel booking'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className} suppressHydrationWarning>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
