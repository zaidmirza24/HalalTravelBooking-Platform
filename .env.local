# LiteAPI Configuration (SERVER-SIDE ONLY - Hidden from browser)
LITEAPI_BASE_URL=https://api.liteapi.travel/v3.0
LITEAPI_KEY=sand_46d727b6-befd-4cf7-b910-27278af8cc86

# API Mode Toggle (CLIENT-SIDE - Accessible in browser)
# Options: 'mock' | 'real' | 'hybrid'
# mock: Use only mock data (for development without API key)
# real: Use only LiteAPI (fails if API errors)
# hybrid: Try LiteAPI first, fallback to mock on error (RECOMMENDED)
NEXT_PUBLIC_API_MODE=real

# Environment (CLIENT-SIDE)
NEXT_PUBLIC_APP_ENV=development
